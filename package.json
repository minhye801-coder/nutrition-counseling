{
  "name": "nutrition-counseling",
  "version": "2.1.0",
  "private": true,
  "type": "module",
  "description": "맛마음 탐험소 Lite · Cloudflare Pages Functions 버전",
  "scripts": {
    "dev": "wrangler pages dev public",
    "check": "node --check public/app.js && node --check functions/_lib/neis.js && node --check functions/api/config.js && node --check functions/api/schools.js && node --check functions/api/meals.js"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
